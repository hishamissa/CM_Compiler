/*
 * Test File: 3.cm
 * Contains 3 syntax errors in statements
 *   Line 14: Missing expression in assignment (x = ;)
 *   Line 17: Invalid statement, random identifiers
 *   Line 19: Missing semicolon after assignment
 * The parser should report "Invalid expression or missing semicolon", "Invalid statement" (twice)
 */

void main(void) {
    int x;
    int y;
    
    x = ;
    
    y = 5;
    bad syntax here;
    
    x = 10
    
    output(y);
}