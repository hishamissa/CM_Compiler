/* Test 4: Syntax errors
   Error 1: missing semicolon after declaration
   Error 2: missing closing parenthesis in if condition
   Error 3: invalid token $ */
void main(void) {
    int x
    int y;
    y = 5;
    if (y > 0 {
        output(y);
    }
    $ = 10;
}
