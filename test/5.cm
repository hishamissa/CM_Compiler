/* Test 5: Semantic errors
   Error 1: variable z used before declaration
   Error 2: variable x declared twice in same scope
   Error 3: function foo called but never defined */
void main(void) {
    int x;
    int x;
    x = z + 1;
    output(foo(x));
}
