int add(int a, int b) {
    return a + b;
}

void printVal(int x) {
    return x;
}

int getVal(void) {
    return;
}

void main(void) {
    int x;
    x = add(5, 10);
    x = add(5);
    x = add(5, 10, 15);
    x = add(true, 10);
    printVal(x);
}